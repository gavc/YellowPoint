name: Build and Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-release:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build -c Release --no-restore

      - name: Publish (framework-dependent)
        run: dotnet publish .\YellowPoint.csproj -c Release -r win-x64 --self-contained false -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -o output/framework-dependent

      - name: Publish (self-contained)
        run: dotnet publish .\YellowPoint.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -o output/self-contained

      - name: Upload artifact (framework-dependent)
        uses: actions/upload-artifact@v4
        with:
          name: yellowpoint-win-x64-framework-dependent
          path: output/framework-dependent/

      - name: Upload artifact (self-contained)
        uses: actions/upload-artifact@v4
        with:
          name: yellowpoint-win-x64-self-contained
          path: output/self-contained/

      - name: Create GitHub Release
        uses: ncipollo/release-action@v1
        with:
          artifacts: "output/**"
          allowUpdates: true
          generateReleaseNotes: true
